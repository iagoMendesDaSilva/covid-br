/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {StackNav} from './src/routes';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StackNav);
